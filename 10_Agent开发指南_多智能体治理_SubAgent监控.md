# 第十章：多智能体治理——何时用、如何选、怎样控

本章讲一件反直觉的事：**大多数场景不需要多智能体**。三个主流平台（OpenAI、Google、Anthropic 技术文档）的一致建议都是从单 Agent + 好 Prompt 开始，只在确实需要时才引入多 Agent。本章从"何时不该用"出发，展开五种协调模式、三种编排架构、上下文拆解方法论、协议生态和生产治理要点。

---

## 10.1 先看成本

多智能体的核心数据只有一组，但已经足够做决策：**+90.2% 性能，15 倍 Token 消耗**。

这不是理论估算。Anthropic Research System 实测，Orchestrator-Worker 架构在处理复杂任务时比单 Agent 强了 90.2%，但 Token 账单是 15 倍。如果你的任务单 Agent 已经能稳定完成，引入多 Agent 只是花钱买了一个更复杂、更难调试的系统。

更具体的决策信号：

**不该用多智能体时**：
- 任务可由单 Agent + 良好 Prompt + 工具调用完成
- 对成本敏感（15x Token 难以承受）
- 还不具备 Agent 级别的可观测性（盲人摸象式调度）
- 缺乏模型分层能力（无法 Brain/Worker 分离——编排用高能力模型、执行用低成本模型）

**应该用时**：
- 任务可清晰分解为并行子任务（Spotify：15-30min → 5-10sec）
- 需要多视角验证（事实校验、代码审查）
- 单一上下文窗口不够用（Chain-of-Agents 用例）
- 需要明确审计链与故障隔离

一个简单判断：**如果你说不出引入多 Agent 解决的是哪个"单 Agent 确实搞不定"的具体瓶颈——那就不要引入。**

---

## 10.2 五种协调模式

2026 年业界已形成五种可落地的协调模式。它们不是"复杂度排行榜"，而是**五道不同的选择题**，分别解决不同瓶颈。

| 模式 | 一句话 | 解决什么瓶颈 | 什么时候够了 |
|------|--------|-------------|-------------|
| **Generator-Verifier** | 一个生成，一个按标准检查 | 输出质量不可控 | 当输出有明确验收标准时 |
| **Orchestrator-Subagent** | 中心拆解任务，短生命子Agent执行 | 上下文过大，主Agent不该读完所有细节 | 当任务可拆为边界清晰的调查问题 |
| **Agent Teams** | 多个长期Worker并行推进各自分区 | 任务多、可分区、每区需多步执行 | 当Worker在同一模块里连续工作积累上下文有价值 |
| **Message Bus** | Agent通过Topic发布/订阅事件 | 流程路径由事件内容决定，新Agent不断接入 | 当中心路由器的if/else分支爆炸 |
| **Shared State** | 多个Agent共同读写结构化知识状态 | 中间发现会互相启发 | 当Agent的发现会实时改变其他Agent的研究方向 |

**演进路径**（不是一步到位）：

```
单Agent + 工具调用
    ↓ 质量不可控时
Generator-Verifier
    ↓ 上下文过载时
Orchestrator-Subagent
    ↓ 需要长期并行时
Agent Teams
    ↓ 路由分支爆炸时
Message Bus
    ↓ 发现需要互相启发时
Shared State
```

> 每增加一层协调机制，都必须对应一个明确的瓶颈。没有质量瓶颈就不要加 Verifier，没有动态路由需求就不要加 Message Bus。复杂协调结构本身也需要测试、监控、调试和维护。

---

## 10.3 三种编排架构：Supervisor vs Swarm vs Hierarchical

协调模式定义的是"怎么协作"，编排架构定义的是"谁来控制"。三种主流架构的选择取决于你优先要**可预测性还是灵活性**。

| 维度 | Supervisor | Swarm | Hierarchical |
|------|-----------|-------|--------------|
| **控制模型** | 单一中心节点全权路由 | 去中心化，Agent自主移交 | 树状层级管理 |
| **延迟（4 Agent任务）** | ~13.5s | ~6s | ~10.5s |
| **容错性** | 低（单点故障） | 高（分布式） | 中（需冗余设计） |
| **可预测性** | 高 | 低 | 高 |
| **调试难度** | 低 | 高 | 中 |
| **典型框架** | LangGraph | OpenAI Swarm | CrewAI |
| **适合场景** | 确定性流水线（3-8 Agent） | 动态对话（2-15 Agent） | 企业级大规模（15-50+ Agent） |

**选型信号**：
- 你明确知道下一步是什么 → **Supervisor**（数据管道、报告生成、内容审核流水线）
- 用户交互路径不可预测 → **Swarm**（客服系统、多轮对话助手）
- Agent 数超 15 个，需要映射组织架构 → **Hierarchical**（企业自动化平台）

大多数项目从 Supervisor 开始。Swarm 的灵活性代价是"不知道系统下一步会干什么"——这在生产环境里不是一个优点，是一个 bug。

---

## 10.4 上下文为中心的任务拆解

这是 2026 年多智能体设计最重要的方法论转变：**不要按职能角色拆 Agent，按上下文边界拆。**

一种常见但危险的拆法是按"角色"拆：一个搜索 Agent、一个写作 Agent、一个审查 Agent、一个总结 Agent。这种拆法的问题在于——它不回答最关键的问题：**哪些信息必须同时出现在同一个上下文窗口里？**

拆解任务前，先做六个判断：

1. **必须同窗的信息**：如果两类信息必须同时出现，强行拆分会让 Agent 丢失判断依据
2. **可隔离的局部任务**：只依赖局部上下文 → 适合 Subagent
3. **会改变方向的中间发现**：会影响其他 Agent 路线 → 需要消息机制或共享状态
4. **可独立验证的输出**：能被规则/测试检查 → 适合生成-验证闭环
5. **固定还是动态的工作流**：路径固定 → 中心编排；事件驱动 → 路由层
6. **是否存在循环风险**：系统可能自我反馈 → 提前设计终止条件

**每个 Agent 都需要输入契约和输出契约**。输入契约说明它能看什么、不能看什么、任务边界在哪；输出契约说明它必须返回什么字段、证据如何引用、置信度如何表达、失败如何报告。没有契约的 Multi-Agent 系统无法调试——你分不清问题出在任务分派、上下文不足、模型推理、工具调用还是结果合成。

---

## 10.5 MCP + A2A 协议生态

2026 年，Multi-Agent 的互操作性正在标准化。两个核心协议已形成分工：

```
应用层
  ↓
A2A (Agent-to-Agent)——水平协调：跨框架 Agent 互操作
  ↓
MCP (Model Context Protocol)——垂直集成：Agent 调用工具/数据
  ↓
外部 API / 数据库 / 文件
```

**MCP**（97M+ 下载）：
- 将 N×M 笛卡尔积简化为 N+M 连接——每个 Agent 不需要单独对接每个工具
- JSON-RPC 2.0（stdio 或 HTTP+SSE）
- 核心原语：Tools / Resources / Prompts / Sampling

**A2A**（Linux Foundation 治理，150+ 合作伙伴，v1.0 于 2026.03 发布）：
- 6 个任务状态：submitted → working → completed/failed/cancelled
- SSE 流式传输
- 让 LangGraph 的 Agent 能调用 CrewAI 的 Agent

**选框架时的协议检查清单**：支持 MCP 吗？支持 A2A 吗？如果不支持——你未来会为"供应商锁定"付出代价。

---

## 10.6 生产治理六要素

多智能体上线后，"正常运行但结果错误"是常态。六项基础设施决定系统能不能生产化。

**1. 限流与超时**：每次 Agent 调用设置超时（建议 30s）+ 指数退避重试（最多 3 次）。不要假设 Agent 会在合理时间内完成。

**2. 熔断器**：某个工具连续失败 N 次 → 暂时屏蔽该工具。不要让一个挂掉的下游 API 拖垮整个 Agent 系统。半开状态允许周期性试探。

**3. Token 预算**：Per-session 硬预算 + 成本异常告警。核心原则：**多个 Agent 共享同一个 BudgetTracker**。如果每个 Agent 各管各的预算，会出现"每个都没超标，总成本超三倍"的问题。

**4. 死循环检测**：两个维度——最大迭代次数硬限制 + 输出相似度检测（滑动窗口比较，超过阈值判停）。不要只检查"完全相等"——Agent 的输出里会夹杂时间戳和随机 ID。

**5. 可观测性**：至少记录六类信息——Agent 调用（输入/输出/Token/耗时）、路由决策（事件类型→目标 Agent）、状态写入（写入者/版本/来源）、验证结果（失败项/迭代次数）、任务生命周期（claimed→running→done→failed→timeout）、最终合成（用了哪些子结果/为什么这样取舍）。使用 OpenTelemetry + Trace ID 贯穿整条调用链。

**6. 优雅降级**：定义各类故障的降级策略——超时→跳过该步用默认值、工具失败→降级到备选工具、成本超标→自动切换低成本模型、全部失败→返回"部分完成"并标注未解决问题。

---

## 10.7 三种常见失败模式

**失败模式一：橡皮图章 Verifier**

Generator-Verifier 模式中，如果 Verifier 的 Prompt 只是"检查这个结果好不好"，模型很容易默认通过。有效 Verifier 必须有明确标准——必须覆盖哪些事实、哪些字段不能为空、哪些测试必须通过。

**失败模式二：信息在传递中蒸发**

Orchestrator 收到 Subagent 的摘要后，原始证据链可能丢失。Subagent 发现的关键细节经过压缩后变成模糊结论，Orchestrator 没有途径追溯原始证据。建议保留关键证据链接或文件位置，对高风险结论保留原始片段。

**失败模式三：过度工程化**

这是最常见的错误——单 Agent + 好 Prompt 能搞定的事情，硬上了 5 个 Agent 的 Orchestrator。结果：成本 15x，延迟 3x，出问题时完全不知道是哪个 Agent 的锅。**复杂性本身不是目标。**

---

## 10.8 决策框架

```
问题：单Agent + 工具调用是否足够？
  ├─ 是 → 停止，不要引入多Agent
  └─ 否 → 瓶颈是什么？
           ├─ 质量不可控 → 加 Generator-Verifier
           ├─ 上下文过载 → 加 Orchestrator-Subagent
           ├─ 需长期并行 → 演化为 Agent Teams
           ├─ 路由分支爆炸 → 引入 Message Bus
           └─ 发现需互相启发 → 引入 Shared State
```

---

## 10.9 小结

多智能体系统的治理，本质上是对**复杂度**的管理——不是对 Agent 的管理。三条规则：

1. **从最简单开始。** 单 Agent 能搞定的，不要上多 Agent。这是 OpenAI、Google、Anthropic 技术文档的一致建议。

2. **成本是一等架构约束。** 15x Token 不是"优化空间"，是"设计起点"。必须在架构层做模型分层、Token 预算和 Prompt Caching。

3. **可观测性先于复杂性。** 增加 Agent 数量前，先确保能追踪每个 Agent 的行为、成本、失败原因。如果系统出了问题你无法从日志里定位到具体是哪个 Agent 做错了什么——那就不应该上线。

多 Agent 的价值不来自数量，而来自恰当地分配上下文、责任和反馈。
