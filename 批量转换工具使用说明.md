# 批量转换工具使用说明

## 📁 文件说明

我为你准备了 2 个 `.bat` 批处理文件:

### 1. `convert-to-pdf.bat` - 批量转换所有文件
**功能**: 自动扫描所有 Markdown 文件并批量转换

**使用方法**:
- 双击运行即可
- 会自动排除 `变现*.md`、`营销*.md` 等文件
- 转换结果保存在 `output/` 目录

### 2. `convert-single.bat` - 转换单个文件
**功能**: 快速转换指定的单个文件

**使用方法**:
```bash
convert-single.bat "文件名.md"
```

**示例**:
```bash
convert-single.bat "01_Agent开发指南_AI平台和api-key管理.md"
```

---

## 🚀 快速开始

### 方法一: 批量转换所有文件 (推荐)

1. **双击运行** `convert-to-pdf.bat`
2. 等待脚本检查依赖
3. 确认开始转换
4. 等待转换完成
5. 自动打开输出目录

### 方法二: 转换单个文件

1. 打开命令行
2. 运行:
   ```bash
   convert-single.bat "01_Agent开发指南_AI平台和api-key管理.md"
   ```
3. 等待转换完成
4. 自动打开输出目录

---

## 📋 前置要求

运行脚本前,请确保已安装:

### 1. Pandoc
**下载地址**: https://pandoc.org/installing.html

**安装方法**:
- Windows: 下载 `.msi` 安装包,双击安装
- 安装完成后重启命令行

**验证安装**:
```bash
pandoc --version
```

### 2. Python + mermaid-filter
**安装方法**:
```bash
pip install mermaid-filter
```

**验证安装**:
```bash
python -c "import mermaid_filter"
```

### 3. LaTeX (MiKTeX 或 TeX Live)
**下载地址**: 
- MiKTeX: https://miktex.org/download (推荐,较小)
- TeX Live: https://www.latex-project.org/get/ (完整版)

**安装方法**:
- Windows: 下载并运行安装包
- 安装时选择"Complete"完整安装

**验证安装**:
```bash
xelatex --version
```

---

## ⚙️ 配置说明

### 修改字体

如果需要使用其他字体,编辑 `.bat` 文件中的以下行:

```batch
-V CJKmainfont="Microsoft YaHei"    REM 中文字体
-V mainfont="Arial"                  REM 英文字体
-V monofont="Consolas"               REM 等宽字体
```

**常用字体**:
- 中文字体: `Microsoft YaHei` (微软雅黑)、`SimHei` (黑体)、`KaiTi` (楷体)
- 英文字体: `Arial`、`Times New Roman`、`Georgia`
- 等宽字体: `Consolas`、`Courier New`、`Fira Code`

### 修改代码高亮主题

编辑 `.bat` 文件中的以下行:

```batch
--highlight-style=tango    REM 可选: zenburn, monokai, kate, etc.
```

**可选主题**:
- `tango` (默认, 推荐)
- `zenburn` (深色)
- `monokai` (彩色)
- `kate` (KDE 风格)
- `pygments` (经典)

---

## 📂 输出结构

转换后的文件保存在 `output/` 目录:

```
agent 开发指南/
├── 00_Agent开发指南_前言.md
├── 01_Agent开发指南_AI平台和api-key管理.md
├── ...
├── convert-to-pdf.bat
├── convert-single.bat
├── pandoc-styles/
│   └── custom-header.tex
└── output/                      ← 转换结果保存在这里
    ├── 00_Agent开发指南_前言.pdf
    ├── 01_Agent开发指南_AI平台和api-key管理.pdf
    └── ...
```

---

## ❓ 常见问题

### Q1: 转换失败,提示 "pandoc 未安装"

**A**: 请先安装 Pandoc:
1. 访问 https://pandoc.org/installing.html
2. 下载 Windows 安装包
3. 双击安装
4. 重启命令行

### Q2: Mermaid 流程图不显示

**A**: 请安装 mermaid-filter:
```bash
pip install mermaid-filter
```

### Q3: 中文乱码或不显示

**A**: 确保 `.bat` 文件中指定了正确的中文字体:
```batch
-V CJKmainfont="Microsoft YaHei"
```

### Q4: PDF 生成失败

**A**: 检查 LaTeX 是否正确安装:
```bash
xelatex --version
```

如果未安装,请下载 MiKTeX: https://miktex.org/download

### Q5: 脚本运行很慢

**A**: 这是正常的,因为:
- LaTeX 编译需要时间
- Mermaid 流程图转换需要时间
- 大文件需要更长时间

**建议**:
- 先用 `convert-single.bat` 测试单个文件
- 确认无误后再批量转换

---

## 🔧 高级用法

### 自定义排除规则

编辑 `convert-to-pdf.bat`,在文件中找到以下部分:

```batch
REM 排除的文件
echo !FILENAME! | findstr /I /C:"变现" >nul
if !errorlevel! neq 0 (
    echo !FILENAME! | findstr /I /C:"营销" >nul
    ...
)
```

添加或修改排除规则:

```batch
echo !FILENAME! | findstr /I /C:"你的关键词" >nul
```

### 修改输出目录

编辑 `.bat` 文件,修改以下行:

```batch
set "OUTPUT_DIR=%SCRIPT_DIR%output"
```

改为:

```batch
set "OUTPUT_DIR=%SCRIPT_DIR%你的目录名"
```

### 转换为 EPUB

编辑 `.bat` 文件,修改 Pandoc 参数:

```batch
REM 修改输出后缀
set "OUTPUT_FILE=%OUTPUT_DIR%\%%~nf.epub"

REM 修改 Pandoc 参数 (移除 LaTeX 相关)
pandoc "!INPUT_FILE!" ^
  -o "!OUTPUT_FILE!" ^
  --filter mermaid-filter ^
  --highlight-style=tango ^
  --toc ^
  --toc-depth=3 ^
  !USE_CUSTOM_HEADER! ^
  -N
```

---

## 💡 使用建议

1. **先测试单个文件**: 使用 `convert-single.bat` 测试一个文件,确认没问题后再批量转换
2. **备份原文件**: 转换前先备份 Markdown 文件
3. **检查输出**: 转换后打开 PDF 检查是否正确
4. **分批转换**: 如果文件很多,可以分批转换,避免一次性转换太多

---

## 📞 技术支持

如果遇到问题:

1. 查看 `Markdown转eBook方案.md` 中的详细说明
2. 检查依赖是否正确安装
3. 检查文件编码是否为 UTF-8
4. 尝试先转换单个文件

---

**祝你转换顺利! 📚**
