# 前言

> "Build agents, not wrappers."

如果你正在读这本书，多半已经在用大语言模型了。可能写过一个带 prompt 的 script，调过几个 API，甚至做过一两个 demo。

但 demo 和产品之间，差着一个 Agent 的距离。

## 本书写给谁

这本书写给要**把 LLM 做成产品**的工程师。不是写给写 prompt 玩的人，不是写给研究模型原理的人，不是写给做业务的产品经理——就是写给工程师。你可能在创业公司搭 MVP，可能在企业的 AI 平台组选型架构，也可能是一个人搞 side project。不管哪种，你的目标都一样：**做出来的东西能在生产环境跑**。

## 什么是 Agent

Agent 的定义很多，本书用工程视角：**Agent 是一个能自主调用工具来达成目标的软件系统**。

拆开来看，三个关键词：

1. **自主**：不是每次等你喂指令，它能自己决定下一步做什么
2. **工具**：不是纯对话，它能查数据库、调 API、写代码、发邮件
3. **目标**：不是无限制自由发挥，它有明确的完成标准

Agent = Model + Harness。模型是大脑（决策和推理），Harness 是身体（工具连接、循环控制、状态管理、记忆存取）。本书讲的就是这个 Harness 层怎么搭。

## 本书讲什么

2026 年的 Agent 开发已经不是 2024 年的"手写一切"。核心变化：

- **协议标准化**：MCP 解决了"模型怎么调工具"，A2A 解决了"Agent 之间怎么合作"
- **推理模型**：模型本身的推理能力使得显式 prompt 提示技巧（"深呼吸"、"一步步思考"）过时了
- **平台成熟**：Dify、Coze、n8n 让非代码场景也能构建合格的 Agent
- **记忆分层**：短期记忆、长期记忆、工作记忆各司其职，不再是简单的"把对话历史全塞进 context"

本书 16 章，分四大块：

**基础层**（01-03 章）：API Key 管理、Context Engineering、结构化输出。Agent 的原材料。

**能力层**（04-08 章）：Function Calling、Skill 体系、短期/长期记忆、RAG。Agent 的肌肉系统。

**智能层**（09-12 章）：规划与推理、多智能体治理、流程编排、生态工具。Agent 的神经系统和交通规则。

**工程层**（16 章）：Harness 与 Loop 工程化。把 Agent 从脚本变成系统工程。

## 本书不讲什么

- 不讲模型底层原理（Transformer、注意力机制、训练方法）——那是 ML 工程师的事
- 不讲特定平台的使用教程（Coze 怎么拖拽、Dify 怎么配置）——那是产品文档干的事
- 不讲某个框架的源码解读——框架会过时，设计思想不会
- 不用虚构的故事和硬造的"踩坑经历"

## 三个贯穿全书的观点

1. **Context Engineering > Prompt Engineering**。设计整个上下文窗口的布局，比调试一段提示词文本重要一个量级。

2. **Agent 是资源调度问题，不是 AI 奇点问题**。管理 token 预算、控制工具调用频率、处理 API 限流——这些"无聊"的工程细节决定了 Agent 能不能跑出 demo。

3. **先选协议，再选框架**。MCP 和 A2A 是基础设施层的标准。框架可以换，协议绑定的是整个生态。

## 关于本书

2026 年 7 月版是本书第三次迭代。如果你看过之前的版本，会发现变化很大——新增了 Harness/Loop 工程化章节，删掉了大量过时的 DIY 实现代码（2024 年你需要手写 JSON 解析器，2026 年是标准功能），术语表从 17 条扩展到 29 条。

一些体例说明：中文正文中首次出现的关键术语会标注英文（如"MCP（模型上下文协议）"）；代码示例以 Python 为主，少量 TypeScript；每章结尾有小结；书末附录有完整术语表。

---

**预备知识**：Python 基础、HTTP 协议基础、用过任一 LLM API。这就够了。
