# 转换失败修复说明

## 🔴 问题原因

你遇到的转换失败有两个原因:

### 1. 中文路径问题
```
pandoc: C:\Users\jike\Desktop\agent 开发指南: withBinaryFile: does not exist
```

LaTeX (xelatex) 在处理包含中文的路径时会出现问题。

### 2. 参数过时警告
```
[WARNING] Deprecated: --highlight-style. Use --syntax-highlighting instead.
```

Pandoc 新版本已经弃用了 `--highlight-style`,需要改成 `--syntax-highlighting`。

---

## ✅ 已修复的内容

### 修复 1: 更新参数名称
- ❌ 旧参数: `--highlight-style=tango`
- ✅ 新参数: `--syntax-highlighting=tango`

### 修复 2: 移除自定义样式文件
自定义样式文件 (`custom-header.tex`) 会引入路径问题,已移除,改用内联参数。

### 修复 3: 切换到相对路径
- 切换到脚本所在目录
- 使用相对路径而不是绝对路径
- 避免中文路径问题

### 修复 4: 添加 LaTeX 参数
```batch
--pdf-opt=-interaction=nonstopmode
--pdf-opt=-halt-on-error
```

让 LaTeX 在遇到错误时不停止,继续尝试完成转换。

---

## 🚀 现在你可以这样做

### 方法一: 使用简化版脚本 (推荐)

**新文件**: `convert-simple.bat`

**特点**:
- ✅ 完全避开中文路径问题
- ✅ 使用相对路径
- ✅ 移除了所有可能导致路径问题的参数
- ✅ 更简洁、更稳定

**使用方法**:
1. 双击 `convert-simple.bat`
2. 确认开始转换
3. 等待完成

---

### 方法二: 使用命令行直接转换

如果脚本还有问题,可以直接用命令行:

```bash
cd "C:\Users\jike\Desktop\agent 开发指南"

# 转换单个文件
pandoc "00_Agent开发指南_前言.md" ^
  -o "output\00_Agent开发指南_前言.pdf" ^
  --pdf-engine=xelatex ^
  -V CJKmainfont="Microsoft YaHei" ^
  -V mainfont="Arial" ^
  -V monofont="Consolas" ^
  --syntax-highlighting=tango ^
  --toc ^
  --toc-depth=3 ^
  -N ^
  --pdf-opt=-interaction=nonstopmode ^
  --pdf-opt=-halt-on-error
```

---

## 📋 关于 Mermaid 流程图

**重要提示**: 由于中文路径问题和 LaTeX 的限制,**Mermaid 流程图可能无法正常渲染**。

### 解决方案:

#### 方案 1: 先转换成图片
使用在线工具把 Mermaid 流程图转换成 PNG 图片:
- https://mermaid.live/
- https://mermaid-js.github.io/mermaid-live-editor/

然后在 Markdown 中引用图片:

```markdown
![流程图](images/flowchart.png)
```

#### 方案 2: 移除 Mermaid 代码块
暂时注释掉 Mermaid 代码块,先用纯文本版本发布,后续再添加图片版本。

#### 方案 3: 使用其他工具
- 使用 Typora (支持 Mermaid,所见即所得)
- 使用 Marp (支持 Mermaid,但适合演示文稿)
- 使用 VitePress (在线文档,支持 Mermaid)

---

## 🔧 进一步调试

如果还是失败,可以这样做:

### 1. 先测试最简单的命令
```bash
cd "C:\Users\jike\Desktop\agent 开发指南"

pandoc "00_Agent开发指南_前言.md" -o "test.pdf"
```

如果这个能成功,说明 Pandoc 没问题,可能是参数的问题。

### 2. 检查文件编码
确保所有 `.md` 文件都是 UTF-8 编码:
- 用记事本打开
- 文件 → 另存为
- 编码选择 "UTF-8"

### 3. 检查字体
确保系统中有这些字体:
- `Microsoft YaHei` (微软雅黑)
- `Arial`
- `Consolas`

如果没有,可以在 `.bat` 文件中改成其他字体。

---

## 💡 最终建议

1. **先测试单个文件**: 用命令行转换单个文件,确认能成功后再批量
2. **暂时不用 Mermaid**: 如果 Mermaid 导致问题,先移除 Mermaid 代码块
3. **使用 Typora 作为备选**: 如果 Pandoc 太难搞,用 Typora 直接导出 PDF 也很方便

---

## 📞 如果还是不行

可以尝试以下备选方案:

### 备选 1: 使用 Typora
1. 用 Typora 打开 Markdown 文件
2. `文件` → `导出` → `PDF`
3. Typora 原生支持 Mermaid 和中文

### 备选 2: 使用 VitePress
创建在线文档,支持 Mermaid,然后打印为 PDF

### 备选 3: 暂时发布 Markdown 版本
先发布 Markdown 版本,后续再生成 PDF

---

**现在试试 `convert-simple.bat` 吧!**

这个版本完全避开了中文路径问题,应该能正常工作。

---

## 📝 知识点记录

### marked v4+ renderer 配置问题

**问题**: 在使用 marked v4+ 时，自定义的 `code` renderer 不生效，导致 mermaid 代码块没有被正确识别和转换。

**错误写法**:
```javascript
// ❌ 不推荐写法
marked.use({
  renderer: {
    code(code, language) {
      if (language === 'mermaid') {
        return `<div class="mermaid-code" ...>${code}</div>`
      }
    }
  }
})
```

**正确写法**:
```javascript
// ✅ 推荐写法：先定义 renderer 对象，再传入
const markedRenderer = {
  code(code, language) {
    if (language === 'mermaid') {
      return `<div class="mermaid-code" ...>${code}</div>`
    }
  }
}
marked.use({ renderer: markedRenderer })
```

**原因**: marked v4+ 对 renderer 对象的处理方式有变化，需要先创建独立对象再传入。

**相关文件**:
- `OutlineEditor.vue` - 大纲编辑器组件
- marked 版本: ^4.3.0
