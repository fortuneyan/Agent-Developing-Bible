# Agent 的自我进化：从工具到伙伴

> 本章是"自我进化"主题的核心概述。专题深度内容请参阅附录 A~J。

---

## 1. 为什么 Agent 需要进化？

大多数 Agent 停留在"被动执行指令"阶段：人类说一句，它做一步，做完就忘。这种模式在生产环境的天花板很明显——Agent 每次都从零开始，无法从历史中学习，遇到重复问题反复踩坑。

自我进化的目标是让 Agent 拥有三个能力：

- **从经验中学习**：记住什么有效、什么无效，下次自动调整
- **从反馈中优化**：用户说"不对"，Agent 不只是重试，而是改进策略
- **持续自我改进**：在没有人类干预的情况下，定期评估自己并作出改进

### 进化等级

| 等级 | 定位 | 特征 | 典型场景 |
|:---|:---|:---|:---|
| L1: 工具 | 被动执行 | 100% 依赖人类指令 | 单次 API 调用 |
| L2: 助手 | 主动建议 | 需人类确认才行动 | ChatGPT/Copilot |
| L3: 伙伴 | 自我学习 | 从交互中积累经验 | OpenClaw、Devin |
| L4: 专家 | 知识创造 | 超越单个人类水平 | 尚未实现 |

**大多数 Agent 停留在 L1-L2。本章探讨 L3 的实现路径。**

### 进化的四个维度

```
自我进化 = 经验积累 + 知识共享 + 安全自修改 + 持续学习
          ↑            ↑            ↑            ↑
       记忆系统    基因协议     沙箱隔离     反馈闭环
```

---

## 2. 核心范式：OpenClaw 的人格文件系统

OpenClaw 是目前最成熟的自我进化 Agent 框架（~180K GitHub Stars，2026年）。它的核心创新很简单：**Agent 的身份和知识存储在 8 个纯文本文件中，Agent 可以读写这些文件**。

```
~/.openclaw/agents/<agentId>/
├── SOUL.md         ← 宪法层：价值观、边界、语气
├── AGENTS.md       ← 操作手册：行为规则、累积知识
├── IDENTITY.md     ← 表面层：名字、emoji、氛围
├── USER.md         ← 用户层：偏好、上下文
├── MEMORY.md       ← 记忆层：跨会话持久知识
├── TOOLS.md        ← 工具层：可用工具说明
├── BOOTSTRAP.md    ← 初始化：首次运行仪式
└── HEARTBEAT.md    ← 心跳层：周期性任务清单
```

### 三层保护机制

| 层级 | 文件 | 规则 |
|:---|:---|:---|
| 受保护（永不删除） | SOUL.md, IDENTITY.md, USER.md, MEMORY.md | 上下文压缩时保留，Agent 可能忘记 40 条消息前的内容，但永远不会忘记自己是谁 |
| 可压缩 | 旧对话轮次 | 压缩为摘要后释放空间 |
| 可删除 | 旧工具输出 | 上下文紧张时首先清除 |

### 记忆刷新（Memory Flush）

这是最精巧的设计。当上下文窗口接近限制时，系统不直接压缩——而是先触发一个特殊轮次：

```
1. 检测上下文接近限制
2. 触发记忆刷新轮次 → Agent 收到提示："把重要的东西保存到 MEMORY.md"
3. Agent 将关键事实写入 MEMORY.md
4. 压缩旧消息
5. 重新注入受保护文件
6. 继续运行，空间已释放
```

### 技能自创建

Agent 不仅能写记忆，还能给自己创建新技能：

```
遇到重复任务 → 创建 workspace/skills/new-skill/
              → 编写 SKILL.md（元数据 + LLM 指令）
              → 编写 scripts/（可执行代码）
              → 下次运行自动发现并使用
```

---

## 3. 安全自修改：Sandbox 隔离

Agent 修改自己的代码存在巨大风险：死循环、误删文件、自我复制失控。解决方案是隔离的沙箱环境。

```
Agent 提议修改 → Sandbox 创建隔离分支 → 自动测试 → 对抗性审查 → 人工批准 → 合并
                                                      ↓
                                                 测试失败 → 回滚
```

**五种隔离技术**：

| 技术 | 隔离级别 | 性能 | 适用场景 |
|:---|:---|:---|:---|
| Firecracker MicroVM | 硬件级 | 高 | 生产级隔离 |
| gVisor | 内核级 | 中 | 容器隔离 |
| Docker Namespace | 进程级 | 高 | 快速测试 |
| Bubblewrap | 用户命名空间 | 高 | Linux 桌面 |
| WASM | 语言级 | 极高 | 轻量执行 |

---

## 4. 跨 Agent 进化：EvoMap 基因协议

单个 Agent 的进化有天花板——它只能从自己的经验中学习。EvoMap 通过 Gene + Capsule 机制实现跨 Agent 的知识共享：

- **Gene（基因）**：经过验证的解决方案片段
- **Capsule（胶囊）**：打包的技能/工具
- **Evolution（进化）**：通过反馈持续改进

Agent A 发现了一个更好的 Prompt 模板 → 发布为 Gene → Agent B 获取并验证 → 如果有效，B 也采纳。整个网络共同进化。

---

## 5. 工程落地要点

### 安全边界

- **硬边界**（不可越过）：系统核心、安全配置、凭证文件
- **软边界**（可修改但需审核）：技能目录、人格文件、记忆文件、源代码

### 关键决策

| 问题 | 建议 |
|:---|:---|
| 什么时候触发自修改？ | 基于失败率阈值（如 >30%）而非固定周期 |
| 自动合并还是人工审批？ | 安全相关必须人工；策略优化可以自动 |
| 如何防止退化？ | 修改前后对比测试 + 可回滚机制 |
| 人格文件能改吗？ | SOUL.md 原则上能改但需双重确认 |

---

## 6. 真实 vs 设想

截至 2026 年：

- ✅ **已实现**：SOUL.md 自修改（OpenClaw）、技能自创建（Foundry）、代码自我修复（SWE-Agent）、多进程 Cluster 热切换
- ⚠️ **部分实现**：自适应模型路由（OpenClaw PR#30185）、RL 训练 Agent（OpenClaw-RL）
- ❌ **未实现**：完全自我改进代码、超越人类专家

---

## 延伸阅读

以下专题附录深入展开各个维度，可按需阅读：

| 附录 | 主题 |
|:---|:---|
| 附录 A | OpenClaw 八文件人格系统详解 |
| 附录 B | EvoMap 基因进化协议与 Q 学习 |
| 附录 C | AI Coding Sandbox 安全沙箱 |
| 附录 D | 完整可运行代码实现指南 |
| 附录 E | 进化系统架构与集群管理 |
| 附录 F | 七层安全防护体系 |
| 附录 G | 测试验证与质量保障 |
| 附录 H | 企业部署、伦理与性能 |
| 附录 I | 真实案例与延伸阅读 |
| 附录 J | 致未来读者：时间胶囊 |
