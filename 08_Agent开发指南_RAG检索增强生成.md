# 第八章：RAG 检索增强生成——2026 的四代演进

本章从"RAG 死了吗"这一 2026 年核心争议出发，展示 RAG 从 Naive 到 Adaptive 的四代演进全景。核心观点：RAG 没有死——它从"因为装不下所以检索"变成了"因为想要精准所以检索"。2026 年的最优实践不是某一种 RAG，而是根据查询复杂度自动路由的 Adaptive RAG。

## 8.1 引言：RAG 死了吗？

2026 年，Gemini 支持 2M Token 上下文窗口，GPT-5.4 支持 1M，Claude 200K。一个自然的问题出现了：**有了这么大的上下文窗口，RAG 还有必要吗？**

答案是：RAG 不仅必要，而且比以往更重要。但它的角色已经变了——从"克服上下文限制的权宜之计"变成了"精准过滤噪音的核心引擎"。

来看一组数据（qubittool.com 2026.04 基准测试）：

| 上下文长度 | 事实召回准确率 | 推理准确率 |
|:---|:---|:---|
| 10K Token | ~99% | ~90% |
| 100K Token | ~92% | ~75% |
| 500K Token | ~85% | ~60% |
| 1M+ Token | ~78% | ~45% |

**Lost in the Middle 效应没有消失**——窗口越大，模型对中间位置信息的注意力越分散。把 1M Token 全部塞进去，推理准确率反而从 90% 掉到 45%。

更关键的是成本。一个 1M Token 知识库：

| 方案 | 单次查询成本 | TTFT |
|:---|:---|:---|
| **全量填塞** | $1.00+ | 15-30s |
| **Context Caching** | $0.10-0.20 | 3-5s |
| **标准 RAG** | <$0.01 | <1s |

**RAG 的成本优势高达 100 倍**。对于每天百万级查询的企业应用，这不是选择问题，是生存问题。

但 RAG 的角色确实变了。2026 年的共识是 **Hybrid 架构**：用 RAG 做检索（找到最相关的 20-50 条片段），用 Long Context 做推理（在精选的 30K Token 上进行深层分析）。这是精准 + 深度的最优平衡。

---

## 8.2 四代 RAG 演进全景

RAG 在 2024-2026 年间经历了四代演进（starmorph.com 基准数据）：

| 代际 | 架构 | 延迟 | 单次成本 | 精度提升(vs Naive) | 最佳场景 |
|:---|:---|:---|:---|:---|:---|
| **Naive RAG** | 向量化 → 检索 top-K → 生成 | 100-500ms | $0.001-0.01 | 基线 | 简单问答、FAQ |
| **Advanced RAG** | +查询变换 +混合检索 +Rerank | 500ms-2s | $0.005-0.03 | +25-40% | 需要高精度的生产系统 |
| **Agentic RAG** | +自主循环检索 → 评估 → 重检索 | 2-10s | $0.01-0.10 | +50-100% | 多跳推理、研究任务 |
| **GraphRAG** | 实体→知识图谱→图遍历→社区摘要 | 1-5s | $0.02-0.15 | 关系查询最高 | 跨文档关系分析 |

**一个关键认知**：不是越高级越好。用 Agentic RAG 回答"退货流程是什么"——答案完全一样，但成本是 Naive RAG 的 10 倍，延迟多 5 秒。这是纯粹的浪费。

---

## 8.3 2026 基础形态：Advanced RAG

Naive RAG（向量数据库 + 一次检索 + 一次生成）在 2026 年已经是新手村的水平。生产环境的"基本款"是 Advanced RAG。它只在检索层做三件事：

**1. 混合检索**：向量检索（语义匹配）+ BM25（精确词汇匹配），通过 RRF 融合两路结果。一个最简单的版本：

```python
# 混合检索 + Rerank 的核心骨架（不是完整实现）
from sentence_transformers import CrossEncoder

def hybrid_retrieve(query, vec_store, bm25_index, top_k=60):
    vec_results = vec_store.search(query, top_k)
    bm25_results = bm25_index.search(query, top_k)
    # RRF 融合（权重可调）
    fused = rrf_fusion([vec_results, bm25_results], k=60)
    return fused[:top_k]

def rerank(query, candidates, model_name="BAAI/bge-reranker-v2-m3"):
    model = CrossEncoder(model_name)
    pairs = [(query, c["text"]) for c in candidates]
    scores = model.predict(pairs)
    return sorted(zip(candidates, scores), key=lambda x: x[1], reverse=True)[:5]
```

**2. Rerank 重排序**：检索 top-60 → Cross-Encoder 精排 → 取 top-5。这是性价比最高的优化——加一个 Reranker，准确率提升 20-30%，成本增加不到 10%。

**3. 查询变换**：用户问"卡得要死怎么办"，系统先自动扩展为"网络延迟高排查方法"+"网速慢解决方案"，多路并行检索。解决了"用户用口语提问，知识库用书面语存储"的语义鸿沟。

这三件事做完，一个 RAG 系统已经能达到 90%+ 的召回率和足够的生产可靠性。**如果你到此为止，不再加任何复杂度，你已经是 2026 年的合格实践者。**

---

## 8.4 2026 进阶形态：Agentic RAG

Agentic RAG 的核心变化：**RAG 从一次性的"检索→生成"变成了一个自主循环**。Agent 可以检索、评估、发现信息不足、换关键词重新检索、综合多轮结果后生成。

五种成熟的 Agentic 模式（starmorph.com 分类）：

| 模式 | 工作原理 | Token 倍数(vs Naive) | 适用场景 |
|:---|:---|:---|:---|
| **迭代检索** | 检索→评估质量→不足则重试 | 2-3x | 简单信息不足时补全 |
| **查询分解** | 复杂问题拆成子问题分别检索 | 3-5x | 多因素对比（如"比较 A 和 B 的定价"） |
| **假设驱动** | 生成假设→检索证据→验证/修正 | 3-5x | 需要证伪的研究任务 |
| **跨语料三角验证** | 多源检索 + 交叉验证 | 5-10x | 必须零幻觉的场景（法律/医疗） |
| **证据加权综合** | 按证据质量加权综合 | 3-5x | 信息源质量参差不齐 |

**什么时候该用 Agentic RAG？** 一个简洁的判断标准：**如果一个查询需要你在 Google 上打开 3 个以上的标签页、对比内容、再得出结论——那就是 Agentic RAG 的场景。** 如果答案就在一个页面的一句话里，Naive/Advanced RAG 就够了。

**代码层面**，Agentic RAG 的实现不再需要手写循环。2026 年直接用 LangGraph 或 CrewAI 的 Agent 循环，定义 `search` 和 `evaluate` 两个工具，剩下的交给 Agent 的 ReAct 循环。这部分已经在第 16 章的 Loop Engineering 中详细讨论过。

---

## 8.5 2026 专项形态：GraphRAG

GraphRAG 不走"向量相似度匹配"的路线，而是先提取文档中的实体和关系，构建知识图谱，然后用图遍历代替向量搜索。

**什么时候该用 GraphRAG：**

- "A 概念在整个文档库中与 B 概念如何关联？"
- "1000 篇文档中，哪些实体被提及最多，形成了什么社区结构？"
- 法规合规分析——需要追踪实体间的关系链

**什么时候是过度设计：**

- 简单事实查找（"文档 X 关于 Y 说了什么？"）
- 单文档问答
- FAQ 机器人

GraphRAG 的索引成本是向量索引的 3-5 倍，且需要额外的图数据库基础设施。微软的 **LazyGraphRAG**（2025 更新）将索引成本降至完整版的 0.1%，大幅降低了准入门槛——但仍只适用于关系型查询。

---

## 8.6 2026 最优实践：Adaptive RAG

把以上四种 RAG 统一到一个自适应系统里，是 2026 年的最优工程实践：

```
用户查询 → 复杂度分类器
              ├── 简单事实查询？→ Naive/Advanced RAG（快、便宜）
              ├── 需要多步推理？→ Agentic RAG（慢、准、贵）
              └── 跨文档关系查询？→ GraphRAG（图遍历）
```

实现上，复杂度分类器本身可以用一个轻量模型（如 GPT-5.4 Nano，$0.15/1M input）判断查询类型：

```python
def classify_query(query: str) -> str:
    """轻量分类器：判断查询应走哪条 RAG 路径"""
    prompt = f"""判断以下查询类型，只输出一个标签：
- simple: 答案在单一文档片段中（如"退货流程是什么"）
- agentic: 需要跨多个文档对比或推理（如"比较A和B的差异"）
- graph: 需要跨文档关系分析（如"A如何影响B在整个系统中的角色"）

查询：{query}
标签："""
    return llm(prompt).strip()
```

**这才是 2026 年 RAG 的真正形态**——不是一个管道，而是一个路由器。绝大多数查询走简单路径（快、便宜），少数复杂查询才启用重量级架构。

---

## 8.7 避坑指南：最常见的三个错误

**错误一：过度工程化。** 这是 RAG 领域最常见的错误——上来就上 Agentic RAG + 知识图谱 + 多轮检索循环。starmorph.com 的原话："最常见的问题不是 RAG 管道工程不足，而是过度工程化。"正确做法：从 Hybrid + Rerank 开始，只有指标证明不够时才往上加复杂度。

**错误二：忽视文档解析质量。** 你把一个 PDF 表格解析成了一堆乱码，后面上再高级的检索技术都没用。入库前跑一个质量检查：文本长度是否合理、表格是否完整、有没有乱码区域。这一关不过，后面的优化全是自欺欺人。

**错误三：用 Agentic RAG 回答简单问题。** "今天天气怎么样"走五轮递归检索——答案和 Naive RAG 一样，但成本是 10 倍，延迟是 5 倍。查询路由是你在上线 RAG 系统第一天就该做的事情。

---

## 8.8 小结

RAG 在 2026 年经历了重要的角色转变：从"因为装不下所以检索"变成"因为想要精准所以检索"。四个关键判断：

1. **对于高频、简单查询**：Advanced RAG（混合检索 + Rerank）够用、快、便宜。
2. **对于复杂推理**：Agentic RAG，但要确保查询复杂度值得这个开销。
3. **对于跨文档关系分析**：GraphRAG，但别把它用在简单事实查找上。
4. **对于生产系统**：Adaptive RAG，让查询路由器决定走哪条路径——这是 2026 年的金标准。

**本章完毕。**
